## 2026-03-05 - US-004
- Changed grid from 25 cols × 40 rows × 20px to 50 cols × 50 rows × 10px (500×500 canvas)
- Canvas now 500×500; CSS container auto-sizes to canvas HTML attributes (no CSS change needed)
- 50×50 grid satisfies AC "row grid should be big maybe 50x50" and "500x500 container"
- Ran typecheck and lint — both pass
- Files changed: src/App.tsx, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - CELL_SIZE=10px with 50×50 grid gives 500×500 canvas matching updated US-004 AC
  - CSS maze-container uses no explicit size — canvas HTML attributes drive the layout
---

## 2026-03-05 - US-006 (iteration 3: stricter collision rules)
- Rewrote collision/resume logic per updated acceptance criteria (Rule A: resume strictly from lastValidPoint)
- On collision: path now freezes at last valid point — invalid point is NOT added to path (no red segment)
- On resume: any pointerdown clears hitWallRef and resumes drawing; subsequent pointermove extends path from lastValidPoint
- Removed validPathLengthRef (no longer needed since path only ever contains valid points)
- Removed snap-to-path resume logic (user no longer needs to click on blue path)
- Updated wall-hit message: "Hit a wall! Click to continue from where you stopped."
- Simplified drawPath and redraw signatures (no hitWall/validLength params)
- Ran typecheck and lint — both pass clean
- Files changed: src/App.tsx, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Never append invalid points to path on collision — freeze in place
  - Resume anchor = tail of pathRef (last valid point); no path truncation needed
  - drawPath doesn't need a red/invalid segment anymore — simpler code
---

## 2026-03-05 - US-006 (iteration 2: resume from path)
- Added `validPathLengthRef` to track the length of the valid (wall-free) portion of the path
- `drawPath` now draws valid portion in blue and wall-hit extension in red as separate segments
- On `pointerdown` when `hitWallRef.current` is true: snaps to nearest point on the valid path within CELL_SIZE radius, truncates path to that point, clears wall-hit state, resumes drawing
- Wall-hit message updated: "Click on the blue path to resume, or clear to start over."
- Files changed: src/App.tsx, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Track valid path length separately from total path length to support partial-path resume
  - Snap radius = CELL_SIZE (20px) gives reliable click-on-path UX without being too forgiving
  - After resume, `validPathLengthRef` resets to the new (shorter) path length so subsequent collisions are tracked correctly
---

## 2026-03-05 - US-008
- Added Usage section to README explaining how to play (Generate → draw from S to E → win)
- Verified: `npm run typecheck` passes (no TS errors), `npm run lint` passes (no ESLint errors), `npm run build` succeeds (Vite + tsc build in ~540ms)
- No code changes required — linting (ESLint flat config) and typecheck were already in place from prior stories
- Files changed: README.md, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - All quality checks (`typecheck`, `lint`, `build`) pass clean — project is in good shape
  - README now has Prerequisites, Install, Run, Usage, and Other commands sections
---

## Codebase Patterns
- React single-page app using Vite + React 19 + TypeScript
- Maze rendered within a fixed 500x800 area
- Pointer Events for input (mouse + touch)
- Path drawing is validated against maze walls (no wall crossing)
- Run with `npm run start` (aliased to `vite`)
- Typecheck with `npm run typecheck` (tsc --noEmit)

## Decisions Log
- Scaffold tool: Vite (`npm create vite@latest` with react-ts template)
- Rendering: TBD (prefer Canvas for drawing + collision)
- Maze algorithm: TBD (recursive backtracker recommended)

## Current State
- Repo is empty
- prd.json created
- Next: scaffold app, confirm run command, render placeholder 500x800 area

---
## 2026-03-05 - US-001
- Scaffolded Vite + React 19 + TypeScript project in repo root using `npm create vite@latest` (react-ts template)
- Added `start` script alias for `vite` in package.json
- Added `typecheck` script (`tsc --noEmit`) in package.json
- Updated README.md with prerequisites, install (`npm install`), and run (`npm run start`) instructions
- Files changed: package.json, README.md, index.html, vite.config.ts, tsconfig*.json, eslint.config.js, src/, public/
- **Learnings for future iterations:**
  - Scaffold tool is Vite; project root contains the app files directly (not in a subdirectory)
  - `npm run start` starts the dev server on `http://localhost:5173`
  - `npm run typecheck` runs `tsc --noEmit` for type checking
  - React 19 is in use (latest as of scaffold)
---

---
## 2026-03-05 - US-002
- Replaced Vite boilerplate in App.tsx with a minimal layout: h1 title, controls div (disabled Generate button placeholder), and a 500x800 `.maze-container` div
- Rewrote App.css: centered flex layout, exact `width:500px; height:800px` on `.maze-container`, `box-sizing:border-box` to prevent overflow
- Added `ralph/**` and `scripts/**` to eslint globalIgnores (those are separate sub-projects that should not be linted by the main config)
- Files changed: src/App.tsx, src/App.css, eslint.config.js
- **Learnings for future iterations:**
  - `.maze-container` uses `flex-shrink: 0` to prevent it from collapsing in flex parents
  - eslint.config.js now ignores `ralph/**` and `scripts/**` subdirectories
  - The disabled "Generate labyrinth" button is a placeholder; wire it up in US-003
---

## Next Iteration Goal
US-008: Polish: README + basic quality checks.

---
## 2026-03-05 - US-007
- Added `won` state to `App.tsx`; detects win when path enters exit cell without crossing a wall
- In `handlePointerMove`, after a valid cell move, checks if `newCell` equals `maze.exit` → sets `won=true`, stops drawing
- Renders a green `.win-banner` with "You solved it!" message and two action buttons: "Generate new labyrinth" and "Draw again"
- "Draw again" calls `handleClearPath` (resets path + won state, keeps maze); "Generate new labyrinth" calls `handleGenerate`
- Both `handleGenerate` and `handleClearPath` reset `won` to false
- Added `.win-banner`, `.win-banner p`, `.win-actions` styles to `App.css`
- Files changed: src/App.tsx, src/App.css, scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Win detection uses the same `currentCellRef` cell-tracking already used for wall collision — no extra state needed
  - The exit cell check runs only after a valid wall-free cell transition, so no false positives
  - Reuse existing button styles (no extra CSS class needed for buttons inside the banner)
---

---
## 2026-03-05 - US-006
- Added cell-based wall collision detection to `src/App.tsx`
- Added `hasWallBetween(maze, from, to)`: walks from→to cell-by-cell (horizontal first), returns true if any wall blocks the path
- Added `posToCell(pos, maze)`: converts pixel position to clamped grid cell
- Added `hitWallRef` and `currentCellRef` refs; `wallHit` state for UI
- On wall collision: stops drawing, turns path red, shows "Hit a wall!" message
- "Clear path" button (already present) resets collision state; user can redraw
- Added `.wall-hit` CSS class in `App.css`
- Files changed: src/App.tsx, src/App.css
- **Learnings for future iterations:**
  - Cell-based collision (track current cell, check walls when crossing cell boundary) is simpler and more reliable than line-segment intersection for grid mazes
  - `hasWallBetween` handles multi-cell jumps (fast pointer movement) by stepping cell-by-cell; horizontal steps are checked before vertical
  - Refs (`hitWallRef`, `currentCellRef`) used for drawing state; minimal `wallHit` boolean state for React UI re-render
  - Path turns red on wall hit — reuse `#ef4444` (already used for exit cell color) for consistency
---

---
## 2026-03-05 - US-005
- Added pointer-event-based drawing to `src/App.tsx`
- Drawing only starts when `pointerdown` lands inside the Start cell (top-left)
- `setPointerCapture` ensures events continue routing to canvas even when pointer leaves element
- Path stored in `pathRef` (ref, not state) to avoid re-renders on every move; `hasPath` boolean state controls "Clear path" button visibility
- Each `pointermove` appends a point and calls `redraw(canvas, maze, path)` which redraws maze base + blue path overlay
- `onPointerCancel` and `onPointerUp` both stop drawing
- `style={{ touchAction: 'none' }}` on canvas prevents browser scroll/zoom from interfering with touch drawing
- "Clear path" button appears only when a path exists; clears without regenerating maze
- Files changed: src/App.tsx
- **Learnings for future iterations:**
  - Use refs (not state) for high-frequency data (path points) to avoid expensive re-renders; use minimal boolean state for UI toggling
  - `setPointerCapture` on `pointerdown` ensures `pointermove`/`pointerup` events are received even outside the element bounds
  - `touchAction: 'none'` on canvas is required to prevent mobile scroll interference
  - Redraw the entire canvas on each move (maze base + path) — Canvas is fast enough at 500×800
---

---
## 2026-03-05 - US-004
- No new code written; all acceptance criteria already satisfied by the US-003 canvas implementation
- Verified: canvas is 500×800px (25cols×20px, 40rows×20px), no overflow; walls clearly drawn; Start/Exit cells visually distinct (green/red + S/E labels); drawMaze() is deterministic from maze state
- Ran `npm run typecheck` and `npm run lint` — both pass
- Marked US-004 `passes: true` in prd.json and committed
- **Learnings for future iterations:**
  - When a previous story already satisfies the next story's acceptance criteria, just verify and mark passing — no need to add code
---

---
## 2026-03-05 - US-003
- Created `src/maze.ts`: `Cell` and `Maze` types, iterative recursive-backtracker `generateMaze(cols, rows)`, `shuffle` helper
- Grid: 25 cols × 40 rows × 20px cell = 500×800 canvas drawing surface
- Start = (0,0) top-left, Exit = (cols-1, rows-1) bottom-right
- Updated `src/App.tsx`: added `useState<Maze|null>`, canvas ref, `drawMaze` (walls + start/exit highlight + S/E labels), wired Generate button, removed `disabled`
- Updated `src/App.css`: replaced `border` + `box-sizing:border-box` with `outline` so canvas HTML attributes (500×800) match CSS display size exactly
- Files changed: src/maze.ts (new), src/App.tsx, src/App.css
- **Learnings for future iterations:**
  - Use iterative backtracker (stack) instead of recursive to avoid call-stack issues on large grids
  - Canvas `width`/`height` attributes set drawing surface; use `outline` (not `border`) on the canvas element to avoid CSS box-model conflicts
  - `window.matchMedia('(prefers-color-scheme: dark)').matches` used at draw time for theme-aware canvas colors
  - Wall drawing: all 4 walls per cell are drawn (shared walls rendered twice, lines overlap cleanly)
  - US-004 rendering criteria are essentially met by this canvas implementation; can be confirmed and marked passing next iteration
---